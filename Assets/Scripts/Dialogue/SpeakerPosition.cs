using UnityEngine;

/// <summary>
/// 화자 위치 (초상화 표시 위치)
/// Speaker Position (For portrait display)
/// </summary>
public enum SpeakerPosition
{
    Left,       // 좌측 / Left side
    Right,      // 우측 / Right side  
    Center,     // 중앙 / Center
    None        // 없음 (내레이션) / None (narration)
}
